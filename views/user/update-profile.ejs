<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="min-h-screen bg-gray-100 flex items-center justify-center px-4">
    <div class="w-full max-w-lg bg-white rounded-xl shadow-lg p-8">
      <h2 class="text-2xl font-bold text-gray-800 text-center mb-6">
        Update Profile
      </h2>

      <form
        action="/user/profile/edit"
        method="POST"
        enctype="multipart/form-data"
        class="space-y-6"
      >
        <!-- Profile Image -->
        <div class="flex flex-col items-center">
          <img
            src="<%= user.profileImage?.secure_url %>"
            alt="Profile Image"
            class="w-28 h-28 rounded-full object-cover border mb-3"
          />

          <label for="profileImage" class="text-sm text-gray-600 font-medium">
            Change Profile Picture
          </label>
          <input
            type="file"
            id="profileImage"
            name="image"
            accept="image/*"
            class="mt-2 block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-md file:border-0 file:text-sm file:font-semibold file:bg-gray-100 file:text-gray-700 hover:file:bg-gray-200"
          />
        </div>

        <!-- Username -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">
            Username
          </label>
          <input
            type="text"
            name="username"
            value="<%= user.username %>"
            required
            class="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-gray-900 focus:outline-none"
          />
        </div>

        <!-- Email (Readonly) -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">
            Email
          </label>
          <input
            type="email"
            value="<%= user.email %>"
            readonly
            class="w-full px-4 py-2 border rounded-lg bg-gray-100 cursor-not-allowed"
          />
        </div>

        <!-- Role (Readonly) -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">
            Role
          </label>
          <input
            type="text"
            value="<%= user.role %>"
            readonly
            class="w-full px-4 py-2 border rounded-lg bg-gray-100 capitalize cursor-not-allowed"
          />
        </div>

        <!-- Account Status -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">
            Account Status
          </label>
          <span
            class="inline-block px-3 py-1 text-sm rounded-full <%= user.isActive ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700' %>"
          >
            <%= user.isActive ? 'Active' : 'Inactive' %>
          </span>
        </div>

        <!-- Submit -->
        <button
          type="submit"
          class="w-full bg-gray-900 text-white py-2 rounded-lg font-semibold hover:bg-gray-800 transition"
        >
          Update Profile
        </button>
      </form>
    </div>
  </body>
</html>
