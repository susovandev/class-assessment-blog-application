<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head.ejs", {title: "Admin Dashboard"}) %>

  <body class="bg-gray-100 min-h-screen flex">
    <!-- Sidebar -->
    <%- include("../admin/sidebar.ejs", {username: admin.username, profileImage: admin.profileImage?.secure_url}) %>

    <!-- Main Content -->
    <main class="flex-1 p-8">
      <!-- Toast Flash -->
      <%- include('../partials/toast') %>

      <!-- Header -->
      <div class="mb-8">
        <h1 class="text-3xl font-bold text-gray-800">Dashboard Overview</h1>
        <p class="text-gray-600 mt-1">Monitor application statistics</p>
      </div>

      <!-- Stats Grid -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-10">
        <!-- Users -->
        <div class="bg-white rounded-xl shadow p-6">
          <p class="text-sm text-gray-500">Total Users</p>
          <h2 class="text-3xl font-bold text-gray-800 mt-2">
            <%= stats?.totalUsers %>
          </h2>
        </div>

        <!-- Categories -->
        <div class="bg-white rounded-xl shadow p-6">
          <p class="text-sm text-gray-500">Total Categories</p>
          <h2 class="text-3xl font-bold text-gray-800 mt-2">
            <%= stats?.totalCategories %>
          </h2>
        </div>

        <!-- Blogs -->
        <div class="bg-white rounded-xl shadow p-6">
          <p class="text-sm text-gray-500">Total Blogs</p>
          <h2 class="text-3xl font-bold text-gray-800 mt-2">
            <%= stats?.totalBlogs %>
          </h2>
        </div>

        <!-- Comments -->
        <div class="bg-white rounded-xl shadow p-6">
          <p class="text-sm text-gray-500">Total Comments</p>
          <h2 class="text-3xl font-bold text-gray-800 mt-2">
            <%= stats?.totalComments %>
          </h2>
        </div>
      </div>

      <!-- Quick Actions -->
      <div class="bg-white rounded-xl shadow p-6">
        <h2 class="text-xl font-semibold text-gray-800 mb-4">Quick Actions</h2>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
          <a
            href="/admin/categories"
            class="p-4 border rounded-lg hover:bg-gray-50 text-center font-medium"
          >
            ➕ Add Category
          </a>

          <a
            href="/admin/blogs/add"
            class="p-4 border rounded-lg hover:bg-gray-50 text-center font-medium"
          >
            ✍️ Create Blog
          </a>

          <a
            href="/admin/users"
            class="p-4 border rounded-lg hover:bg-gray-50 text-center font-medium"
          >
            👥 View Users
          </a>

          <a
            href="/admin/comments"
            class="p-4 border rounded-lg hover:bg-gray-50 text-center font-medium"
          >
            💬 Manage Comments
          </a>
        </div>
      </div>
    </main>
  </body>
</html>
